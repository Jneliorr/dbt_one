
version: 2

models:
    - name: select_itaborai
      description: "verifica_municipio"
      columns:
          - name: MUNICIPIO
            description: "possuir o MUNICIPIO"
            tests:
                - accepted_values:
                    values: ['5837']

